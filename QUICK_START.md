# ATM-Go 快速开始指南

## 🎉 恭喜！项目已经构建成功！

可执行文件位置：`bin/atm.exe` (大小: 9.7MB)

## 快速测试

### 1. 直接运行（无需安装）

```bash
# 在项目目录下
cd D:\go_pro\ai_tools_manager\atm-go
.\bin\atm.exe
```

### 2. 全局安装（推荐）

让 `atm` 命令在任何位置都能使用：

**方法 1: 复制到系统目录（需要管理员权限）**
```bash
copy bin\atm.exe C:\Windows\System32\
```

**方法 2: 添加到 PATH**
1. 右键"此电脑" -> "属性"
2. "高级系统设置" -> "环境变量"
3. 在"系统变量"中找到 `Path`
4. 添加: `D:\go_pro\ai_tools_manager\atm-go\bin`
5. 重启命令行窗口

安装后验证：
```bash
atm
```

## 使用说明

### 基本操作流程

1. **启动程序**
   ```bash
   atm
   ```

2. **主菜单选项**
   - **安装工具** - 安装尚未安装的 AI 工具
   - **查询工具** - 查看已安装工具的版本和更新状态
   - **更新工具** - 更新有新版本的工具
   - **卸载工具** - 移除不需要的工具
   - **退出** - 退出程序

3. **操作提示**
   - 使用 **方向键** 上下选择
   - 按 **回车键** 确认选择
   - 某些界面支持 **空格键** 多选

### 功能演示

#### 查询已安装的工具
```bash
atm
# 选择: 查询工具
# 显示: 所有已安装的 AI 工具及其版本信息
```

#### 安装新工具
```bash
atm
# 选择: 安装工具
# 从列表中选择要安装的工具
# 等待安装完成
```

#### 更新工具
```bash
atm
# 选择: 更新工具
# 如果有可更新的工具，会显示版本对比（v1.0 → v2.0）
# 选择要更新的工具
```

## 配置

### 环境变量

**禁用版本检查**
```bash
set ATM_SKIP_VERSION_CHECK=true
atm
```

**强制使用英文界面**
```bash
set LANG=en_US.UTF-8
atm
```

**强制使用中文界面**
```bash
set LANG=zh_CN.UTF-8
atm
```

### 添加自定义工具

编辑 `config/tools.json`（或 `pkg/config/tools.json`）：

```json
{
  "tools": [
    {
      "name": "你的工具名称",
      "package": "npm-package-name",
      "description": "工具描述"
    }
  ]
}
```

修改后需要重新编译：
```bash
go build -o bin/atm.exe cmd/atm/main.go
```

## 开发相关

### 开发模式运行
```bash
go run cmd/atm/main.go
```

### 重新编译
```bash
# 普通编译
go build -o bin/atm.exe cmd/atm/main.go

# 优化编译（减小文件大小）
go build -ldflags="-s -w" -o bin/atm.exe cmd/atm/main.go
```

### 使用 Make 构建
```bash
# 构建
make build

# 运行
make run

# 清理
make clean

# 跨平台编译
make cross-compile
```

### 使用构建脚本
```bash
# Windows
cd build
.\build.bat

# Linux/Mac
cd build
chmod +x build.sh
./build.sh
```

## 系统要求

### 运行时要求
- **npm** - 必须安装（因为管理的是 npm 包）
- **Windows 7+** 或其他支持的操作系统
- 无需安装 Go 运行时（已编译为独立可执行文件）

### 开发要求
- **Go 1.21+**
- **npm**
- **Git** （用于版本控制）

## 目录结构

```
atm-go/
├── bin/
│   └── atm.exe          ← 编译后的可执行文件（9.7MB）
├── cmd/
│   └── atm/
│       └── main.go      ← 程序入口
├── pkg/
│   ├── app/             ← 主应用逻辑
│   ├── config/          ← 配置加载（含 tools.json）
│   ├── i18n/            ← 国际化支持
│   ├── manager/         ← NPM 包管理
│   └── version/         ← 版本检查
├── config/
│   └── tools.json       ← 工具配置文件
├── build/
│   ├── build.bat        ← Windows 构建脚本
│   └── build.sh         ← Linux/Mac 构建脚本
├── go.mod               ← Go 模块定义
├── Makefile             ← Make 构建配置
├── README.md            ← 用户文档
├── DEV_GUIDE.md         ← 开发指南
├── ARCHITECTURE.md      ← 架构设计
└── QUICK_START.md       ← 快速开始（本文件）
```

## 支持的 AI 工具

当前支持 **11** 个 AI 开发工具：

1. **Claude Code** - @anthropic-ai/claude-code
2. **Qwen Code** - @qwen-code/qwen-code
3. **Code Buddy** - @tencent-ai/codebuddy-code
4. **Gemini CLI** - @google/gemini-cli
5. **Auggie** - @augmentcode/auggie
6. **Crush** - @charmland/crush
7. **Codex** - @openai/codex
8. **iFlow** - @iflow-ai/iflow-cli
9. **OpenCode** - opencode-ai
10. **Copilot CLI** - @github/copilot
11. **Kode** - @shareai-lab/kode

## 常见问题

### Q: 提示"npm 命令不存在"？
**A:** 需要先安装 Node.js 和 npm。下载地址：https://nodejs.org/

### Q: 中文显示乱码？
**A:** 在 Windows 命令行执行：
```bash
chcp 65001
```

### Q: 如何卸载？
**A:** 如果安装到了系统目录：
```bash
del C:\Windows\System32\atm.exe
```

### Q: 如何更新 ATM 本身？
**A:** 重新编译并替换可执行文件：
```bash
go build -ldflags="-s -w" -o bin/atm.exe cmd/atm/main.go
copy bin\atm.exe C:\Windows\System32\
```

### Q: 可以管理非 npm 的工具吗？
**A:** 当前版本仅支持 npm 全局包。未来可能支持其他包管理器。

## 下一步

- 📖 阅读 [DEV_GUIDE.md](DEV_GUIDE.md) 了解开发详情
- 🏗️ 阅读 [ARCHITECTURE.md](ARCHITECTURE.md) 了解架构设计
- 🐛 遇到问题？提交 Issue
- 💡 有想法？提交 Pull Request

## 版本信息

- **当前版本**: v1.0.0
- **Go 版本**: 1.21.3
- **构建时间**: 2025-11-11

---

享受使用 ATM-Go！🎉
